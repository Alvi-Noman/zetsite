# .gitignore
node_modules/
.pnpm-store/
.next/
dist/
build/
.turbo/
.env
.env.*
.env*.local
!.env.example
*.timestamp-*.mjs
*.log
*.zip
.vscode/
.idea/
.DS_Store
*.ps1
# Local-only compose overrides (e.g. a local db container for dev) — must never reach a real
# server, since docker compose auto-loads this file if present.
docker-compose.override.yml
